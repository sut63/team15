// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team15/app/ent/facility"
)

// Facility is the model entity for the Facility schema.
type Facility struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Facility holds the value of the "facility" field.
	Facility string `json:"facility,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FacilityQuery when eager-loading is set.
	Edges FacilityEdges `json:"edges"`
}

// FacilityEdges holds the relations/edges for other nodes in the graph.
type FacilityEdges struct {
	// Room holds the value of the room edge.
	Room []*Room
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RoomOrErr returns the Room value or an error if the edge
// was not loaded in eager-loading.
func (e FacilityEdges) RoomOrErr() ([]*Room, error) {
	if e.loadedTypes[0] {
		return e.Room, nil
	}
	return nil, &NotLoadedError{edge: "room"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Facility) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // facility
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Facility fields.
func (f *Facility) assignValues(values ...interface{}) error {
	if m, n := len(values), len(facility.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	f.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field facility", values[0])
	} else if value.Valid {
		f.Facility = value.String
	}
	return nil
}

// QueryRoom queries the room edge of the Facility.
func (f *Facility) QueryRoom() *RoomQuery {
	return (&FacilityClient{config: f.config}).QueryRoom(f)
}

// Update returns a builder for updating this Facility.
// Note that, you need to call Facility.Unwrap() before calling this method, if this Facility
// was returned from a transaction, and the transaction was committed or rolled back.
func (f *Facility) Update() *FacilityUpdateOne {
	return (&FacilityClient{config: f.config}).UpdateOne(f)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (f *Facility) Unwrap() *Facility {
	tx, ok := f.config.driver.(*txDriver)
	if !ok {
		panic("ent: Facility is not a transactional entity")
	}
	f.config.driver = tx.drv
	return f
}

// String implements the fmt.Stringer.
func (f *Facility) String() string {
	var builder strings.Builder
	builder.WriteString("Facility(")
	builder.WriteString(fmt.Sprintf("id=%v", f.ID))
	builder.WriteString(", facility=")
	builder.WriteString(f.Facility)
	builder.WriteByte(')')
	return builder.String()
}

// Facilities is a parsable slice of Facility.
type Facilities []*Facility

func (f Facilities) config(cfg config) {
	for _i := range f {
		f[_i].config = cfg
	}
}
