// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team15/app/ent/pledge"
)

// Pledge is the model entity for the Pledge schema.
type Pledge struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Provision holds the value of the "provision" field.
	Provision string `json:"provision,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PledgeQuery when eager-loading is set.
	Edges PledgeEdges `json:"edges"`
}

// PledgeEdges holds the relations/edges for other nodes in the graph.
type PledgeEdges struct {
	// Roomdetails holds the value of the roomdetails edge.
	Roomdetails []*Roomdetail
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RoomdetailsOrErr returns the Roomdetails value or an error if the edge
// was not loaded in eager-loading.
func (e PledgeEdges) RoomdetailsOrErr() ([]*Roomdetail, error) {
	if e.loadedTypes[0] {
		return e.Roomdetails, nil
	}
	return nil, &NotLoadedError{edge: "roomdetails"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Pledge) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // provision
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Pledge fields.
func (pl *Pledge) assignValues(values ...interface{}) error {
	if m, n := len(values), len(pledge.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	pl.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field provision", values[0])
	} else if value.Valid {
		pl.Provision = value.String
	}
	return nil
}

// QueryRoomdetails queries the roomdetails edge of the Pledge.
func (pl *Pledge) QueryRoomdetails() *RoomdetailQuery {
	return (&PledgeClient{config: pl.config}).QueryRoomdetails(pl)
}

// Update returns a builder for updating this Pledge.
// Note that, you need to call Pledge.Unwrap() before calling this method, if this Pledge
// was returned from a transaction, and the transaction was committed or rolled back.
func (pl *Pledge) Update() *PledgeUpdateOne {
	return (&PledgeClient{config: pl.config}).UpdateOne(pl)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (pl *Pledge) Unwrap() *Pledge {
	tx, ok := pl.config.driver.(*txDriver)
	if !ok {
		panic("ent: Pledge is not a transactional entity")
	}
	pl.config.driver = tx.drv
	return pl
}

// String implements the fmt.Stringer.
func (pl *Pledge) String() string {
	var builder strings.Builder
	builder.WriteString("Pledge(")
	builder.WriteString(fmt.Sprintf("id=%v", pl.ID))
	builder.WriteString(", provision=")
	builder.WriteString(pl.Provision)
	builder.WriteByte(')')
	return builder.String()
}

// Pledges is a parsable slice of Pledge.
type Pledges []*Pledge

func (pl Pledges) config(cfg config) {
	for _i := range pl {
		pl[_i].config = cfg
	}
}
