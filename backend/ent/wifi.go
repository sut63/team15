// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team15/app/ent/wifi"
)

// Wifi is the model entity for the Wifi schema.
type Wifi struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Wifiname holds the value of the "wifiname" field.
	Wifiname string `json:"wifiname,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WifiQuery when eager-loading is set.
	Edges WifiEdges `json:"edges"`
}

// WifiEdges holds the relations/edges for other nodes in the graph.
type WifiEdges struct {
	// Wifis holds the value of the wifis edge.
	Wifis []*Lease
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// WifisOrErr returns the Wifis value or an error if the edge
// was not loaded in eager-loading.
func (e WifiEdges) WifisOrErr() ([]*Lease, error) {
	if e.loadedTypes[0] {
		return e.Wifis, nil
	}
	return nil, &NotLoadedError{edge: "wifis"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Wifi) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // wifiname
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Wifi fields.
func (w *Wifi) assignValues(values ...interface{}) error {
	if m, n := len(values), len(wifi.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	w.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field wifiname", values[0])
	} else if value.Valid {
		w.Wifiname = value.String
	}
	return nil
}

// QueryWifis queries the wifis edge of the Wifi.
func (w *Wifi) QueryWifis() *LeaseQuery {
	return (&WifiClient{config: w.config}).QueryWifis(w)
}

// Update returns a builder for updating this Wifi.
// Note that, you need to call Wifi.Unwrap() before calling this method, if this Wifi
// was returned from a transaction, and the transaction was committed or rolled back.
func (w *Wifi) Update() *WifiUpdateOne {
	return (&WifiClient{config: w.config}).UpdateOne(w)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (w *Wifi) Unwrap() *Wifi {
	tx, ok := w.config.driver.(*txDriver)
	if !ok {
		panic("ent: Wifi is not a transactional entity")
	}
	w.config.driver = tx.drv
	return w
}

// String implements the fmt.Stringer.
func (w *Wifi) String() string {
	var builder strings.Builder
	builder.WriteString("Wifi(")
	builder.WriteString(fmt.Sprintf("id=%v", w.ID))
	builder.WriteString(", wifiname=")
	builder.WriteString(w.Wifiname)
	builder.WriteByte(')')
	return builder.String()
}

// Wifis is a parsable slice of Wifi.
type Wifis []*Wifi

func (w Wifis) config(cfg config) {
	for _i := range w {
		w[_i].config = cfg
	}
}
